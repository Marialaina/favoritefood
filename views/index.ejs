<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/style.css">
        <title></title>
    </head>
    <body>
        <h1>Dishes to Cook</h1>
        <a href="/meals/new" class="new-dish">Create a New Dish</a>
        <main style="list-style-type:none;">
            <% for(let i = 0; i < meals.length; i++){ %>
                <article id = "formcontainer">
                    <a href="/meals/<%=meals[i].id; %>" class="specific-dish"><%=meals[i].name; %></a>
                    <img src="<%= meals[i].img %>" >
                    <% if(meals[i].cookedDish === true){ %>
                        It is ready to eat
                    <% } else { %>
                        It is not ready to eat
                    <% } %>
                    <a href="/meals/<%=meals[i].id; %>/edit">Edit</a>
                      <!--  ADD DELETE FORM HERE-->
                    <form action="/meals/<%=meals[i].id; %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE"/>
                </form>
                </article>
                
            <% } %>
        </main>
    </body>
</html>